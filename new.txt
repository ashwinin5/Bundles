Due to the way that Git works, the file you want to track with LFS (myfile) cannot be staged before it is "tracked" via your .gitattributes. To fix this, you can do a hard reset to before you added the file and run git lfs track myfile before you git add myfile. I noticed that that is the order it seems you ran those commands, but if the file myfile is already staged, subsequent git adds are treated as n